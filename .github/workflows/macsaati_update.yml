name: Mac Saati Otomatik Fikstür Güncelleyici

on:
  schedule:
    - cron: '*/15 * * * *' # 🚀 Hedeflediğin gibi 15 dakikada bire çektim
  workflow_dispatch: 

jobs:
  update-data:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Repoyu Klonla
        uses: actions/checkout@v4
        with:
          fetch-depth: 1 

      - name: Node.js Kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm' 

      # 🧹 apt-get update tamamen silindi! Ubuntu-latest zaten Puppeteer destekler.

      - name: Kütüphaneleri Yükle
        run: npm ci 

      # ==========================================
      # 🚀 VERİ ÇEKME (PARALEL MOD)
      # ==========================================
      - name: Tüm Spor Verilerini Çek
        run: |
          node fetch_football.js &
          node fetch_basketball.js &
          node fetch_tennis.js &
          wait

      # 🧹 logo_masters.js buradan silindi. Onu günde 1 kez çalışacak ayrı bir action yapacağız.

      # ==========================================
      # 📤 GITHUB'A PUSH
      # ==========================================
      - name: Değişiklikleri Kontrol Et ve Pushla
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .
          
          if git diff --staged --quiet; then
            echo "Güncellenecek yeni bir veri bulunamadı."
          else
            git commit -m "Otomatik Güncelleme: $(date +'%d-%m-%Y %H:%M')"
            # Push işlemini hızlandırmak ve çakışmaları önlemek için rebase mantığı eklendi
            git pull origin main --rebase
            git push origin main
            echo "✅ Veriler başarıyla GitHub'a gönderildi."
          fi
